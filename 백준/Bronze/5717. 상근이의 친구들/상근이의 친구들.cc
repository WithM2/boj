#include<bits/stdc++.h>
using namespace std;

#define pii pair<int, int>

int main(){

    int m, f;
    while (true) {
        cin >> m >> f;
        if (m == 0 && f == 0) break;

        cout << m + f << "\n";


    }
    
    

    return 0;
}